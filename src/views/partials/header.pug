header.header__container
    div.header__title
        a(href="/").header__logo
            i.fas.fa-dragon
        span Test

    div.header__search
        form(method="GET", action="/search").search__form
            input(placeholder="제목 검색",name="keyword", type="text")
            button
                i.fas.fa-search
    nav
        ul
            
            if loggedIn
                li
                    a(href="/videos/upload")
                        i.fas.fa-video
                li
                    a(href="/users/logout") 로그 아웃
                li
                    a(href=`/users/${loggedInUser._id}`)
                        if !loggedInUser.avatarUrl
                            img.header__avatar(src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcR3MAdEhs1pDTSXdLl7RZn9t_WwnWNwsnt5ZA&usqp=CAU")  
                        else
                            img.header__avatar(src=`/${loggedInUser.avatarUrl}`)
            else
                li
                    a(href="/login") 로그인
                li 
                    a(href="/join").header__btn 회원 가입