extends ../base
include ../mixins/video

block content
    div.profile__data
        div
        div.profile__data-user
            if user.avatarUrl=="{}"
                img.header__avatar.avatar(src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcR3MAdEhs1pDTSXdLl7RZn9t_WwnWNwsnt5ZA&usqp=CAU")
            else
                img.header__avatar(src="/" + user.avatarUrl).avatar
            h3=user.name
        div.profile__data-btns
            a(href="/users/edit") 프로필 수정

    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message 비디오를 찾을 수 없습니다.